describe('suite', () => {
    beforeEach(() => {
        console.log('before each body');
    });

    it('test', () => {
       console.log('test body');
    });

    afterEach(() => {
       console.log('after each body');
    });
});
